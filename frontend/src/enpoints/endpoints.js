const API_BASE = "http://localhost:3001/api";

// Login y autenticación
export const LOGIN = `${API_BASE}/auth/login`;

// Productos
export const PRODUCTOS = `${API_BASE}/productos`;

// Usuarios
export const USUARIOS = `${API_BASE}/usuarios`;

// Ventas
export const VENTAS = `${API_BASE}/ventas`;

// Asistencias
export const ASISTENCIAS = `${API_BASE}/asistencias`;

// Ingresos
export const INGRESOS = `${API_BASE}/ingresos`;